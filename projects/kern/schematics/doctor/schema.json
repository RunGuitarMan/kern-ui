{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "KernDoctor",
  "title": "KERN workspace check options",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "project": {
      "type": "string",
      "description": "Angular application to inspect. All applications are checked when omitted."
    },
    "fix": {
      "type": "boolean",
      "default": false,
      "description": "Add the global KERN style entry when it is missing."
    },
    "strict": {
      "type": "boolean",
      "default": false,
      "description": "Fail when styles are missing or duplicated; useful in CI."
    }
  }
}
