# @kern-ui/showcase

Private support library for the Kern documentation application. It owns the typed 132-entry
catalog, interactive playground metadata, deterministic specimens, API metadata, state matrices,
keyboard contracts, and coverage counters. It is intentionally not a public component package.
