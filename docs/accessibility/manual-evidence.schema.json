{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/RunGuitarMan/kern-ui/blob/main/docs/accessibility/manual-evidence.schema.json",
  "title": "Kern manual accessibility evidence",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "schemaVersion",
    "libraryVersion",
    "policy",
    "certification",
    "targetComponentIds",
    "records"
  ],
  "properties": {
    "$schema": {
      "const": "./manual-evidence.schema.json"
    },
    "schemaVersion": {
      "const": 3
    },
    "libraryVersion": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z.-]+)?$"
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pre1ReleaseAllowsPending", "releaseMaxAgeDays", "promotionMaxAgeDays"],
      "properties": {
        "pre1ReleaseAllowsPending": {
          "const": true
        },
        "releaseMaxAgeDays": {
          "type": "integer",
          "minimum": 1
        },
        "promotionMaxAgeDays": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "certification": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "statement", "attestedAt", "attestedBy", "evidence"],
      "properties": {
        "status": {
          "enum": ["not-certified", "certified"]
        },
        "statement": {
          "type": "string",
          "minLength": 20
        },
        "attestedAt": {
          "type": ["string", "null"],
          "format": "date-time"
        },
        "attestedBy": {
          "type": ["string", "null"],
          "minLength": 2
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "targetComponentIds": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "records": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/record"
      }
    }
  },
  "definitions": {
    "namedVersion": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "version"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "version": {
          "type": ["string", "null"],
          "minLength": 1
        }
      }
    },
    "environment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["operatingSystem", "browser", "assistiveTechnology", "input"],
      "properties": {
        "operatingSystem": {
          "$ref": "#/definitions/namedVersion"
        },
        "browser": {
          "$ref": "#/definitions/namedVersion"
        },
        "assistiveTechnology": {
          "$ref": "#/definitions/namedVersion"
        },
        "input": {
          "$ref": "#/definitions/namedVersion"
        }
      }
    },
    "record": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "purpose",
        "componentIds",
        "environment",
        "checks",
        "status",
        "required",
        "releaseBlocking",
        "testedAt",
        "tester",
        "verifiedBy",
        "evidence",
        "notes"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
        },
        "purpose": {
          "type": "string",
          "minLength": 20
        },
        "componentIds": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "environment": {
          "$ref": "#/definitions/environment"
        },
        "checks": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": [
              "name-role-value",
              "keyboard",
              "focus-order",
              "focus-restoration",
              "announcements",
              "browse-mode",
              "forms-mode",
              "high-contrast",
              "zoom-200",
              "zoom-400",
              "reflow",
              "reduced-motion"
            ]
          }
        },
        "status": {
          "enum": ["pending", "pass", "fail", "blocked"]
        },
        "required": {
          "type": "boolean"
        },
        "releaseBlocking": {
          "type": "boolean"
        },
        "testedAt": {
          "type": ["string", "null"],
          "format": "date-time"
        },
        "tester": {
          "type": ["string", "null"],
          "minLength": 2
        },
        "verifiedBy": {
          "type": ["string", "null"],
          "minLength": 2
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "notes": {
          "type": "string",
          "minLength": 10
        }
      }
    }
  }
}
